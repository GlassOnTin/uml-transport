diff --git a/dhcp.c b/dhcp.c
index c3c7422..a248db2 100644
--- a/dhcp.c
+++ b/dhcp.c
@@ -318,21 +318,38 @@ int dhcp(const struct ctx *c, struct iov_tail *data)
 
 	eh = IOV_REMOVE_HEADER(data, eh_storage);
 	iph = IOV_PEEK_HEADER(data, iph_storage);
-	if (!eh || !iph)
+	if (!eh || !iph) {
+		warn("DHCP: frame dropped: no ethernet/IPv4 header");
 		return -1;
+	}
 
-	if (!iov_drop_header(data, iph->ihl * 4UL))
+	if (!iov_drop_header(data, iph->ihl * 4UL)) {
+		warn("DHCP: frame dropped: truncated IPv4 header");
 		return -1;
+	}
 
 	uh = IOV_REMOVE_HEADER(data, uh_storage);
-	if (!uh)
+	if (!uh) {
+		warn("DHCP: frame dropped: truncated UDP header");
 		return -1;
+	}
 
 	if (uh->dest != htons(67))
 		return 0;
 
-	if (c->no_dhcp)
+	warn("DHCP: request from %s, udp len %u",
+	     eth_ntop((const unsigned char *)eh->h_dest, macstr, sizeof(macstr)),
+	     ntohs(uh->len));
+
+	if (c->no_dhcp) {
+		char a[INET_ADDRSTRLEN], b[INET_ADDRSTRLEN];
+		warn("DHCP: server disabled (no_dhcp, ifi4 %d, addr %s, gw %s, prefix %u)",
+		     c->ifi4,
+		     inet_ntop(AF_INET, &c->ip4.addr, a, sizeof(a)),
+		     inet_ntop(AF_INET, &c->ip4.guest_gw, b, sizeof(b)),
+		     c->ip4.prefix_len);
 		return 1;
+	}
 
 	mlen = iov_tail_size(data);
 	m = (struct msg const *)iov_remove_header_(data, &m_storage,
@@ -341,8 +358,11 @@ int dhcp(const struct ctx *c, struct iov_tail *data)
 	if (!m						||
 	    mlen  != ntohs(uh->len) - sizeof(*uh)	||
 	    mlen  <  offsetof(struct msg, o)		||
-	    m->op != BOOTREQUEST)
+	    m->op != BOOTREQUEST) {
+		warn("DHCP: request validation failed (mlen %zu, udp len %u, op %u)",
+		     mlen, ntohs(uh->len), m ? m->op : 0);
 		return -1;
+	}
 
 	reply.op		= BOOTREPLY;
 	reply.htype		= m->htype;
@@ -368,8 +388,11 @@ int dhcp(const struct ctx *c, struct iov_tail *data)
 		const uint8_t *olen;
 		uint8_t *type;
 
-		if (!(type = IOV_REMOVE_HEADER(data, type_storage)))
+		if (!(type = IOV_REMOVE_HEADER(data, type_storage))) {
+			warn("DHCP: option parse: truncated type at %zu bytes left",
+			     opt_len);
 			return -1;
+		}
 
 		if (*type == 255)
 			break;
@@ -377,11 +400,17 @@ int dhcp(const struct ctx *c, struct iov_tail *data)
 		if (*type == 0) /* Pad Option (RFC 2132, 3.1): one byte */
 			continue;
 
-		if (!(olen = IOV_REMOVE_HEADER(data, olen_storage)))
+		if (!(olen = IOV_REMOVE_HEADER(data, olen_storage))) {
+			warn("DHCP: option parse: truncated length (type %u)",
+			     *type);
 			return -1;
+		}
 
-		if (opt_len - 2 < *olen)
+		if (opt_len - 2 < *olen) {
+			warn("DHCP: option parse: len %u overruns message (left %zu, type %u)",
+			     *olen, opt_len - 2, *type);
 			return -1;
+		}
 
 		iov_to_buf(&data->iov[0], data->cnt, data->off, &opts[*type].c, *olen);
 		opts[*type].clen = *olen;
@@ -403,6 +432,8 @@ int dhcp(const struct ctx *c, struct iov_tail *data)
 		     (opts[53].clen <= 0) ? "BOOTP" : "DHCP");
 		opts[53].s[0] = DHCPACK;
 	} else {
+		warn("DHCP: unexpected message type %u (opts[53].clen %d)",
+		     opts[53].clen > 0 ? opts[53].c[0] : 0, opts[53].clen);
 		return -1;
 	}
 
diff --git a/isolation.c b/isolation.c
index a00475f..557e859 100644
--- a/isolation.c
+++ b/isolation.c
@@ -384,6 +384,27 @@ void isolate_user(const struct ctx *c, uid_t uid, gid_t gid, bool use_userns,
 {
 	uint64_t ns_caps = 0;
 
+	/* With no user namespaces in the kernel (Android), passt's normal
+	 * privilege drop via a userns is impossible. When already
+	 * unprivileged, dropping root isn't needed, so skip it.
+	 */
+	if (use_userns && getenv("PASST_NO_SANDBOX")) {
+		warn("PASST_NO_SANDBOX set, skipping user namespace");
+		use_userns = false;
+	}
+
+	/* Android app processes already run as an unprivileged uid, and the
+	 * app seccomp filter force-kills setgroups rather than returning
+	 * EPERM; the app uid has no capabilities for drop_caps_ep_except()
+	 * to drop either. With the sandbox opted out and no userns in play
+	 * there is nothing left for the rest of this function to do.
+	 */
+	if (!use_userns && !*userns && getenv("PASST_NO_SANDBOX") &&
+	    geteuid() != 0) {
+		warn("PASST_NO_SANDBOX set, skipping unneeded privilege drop");
+		return;
+	}
+
 	/* First set our UID & GID in the original namespace */
 	if (setgroups(0, NULL)) {
 		/* If we don't have CAP_SETGID, this will EPERM */
@@ -479,6 +500,15 @@ int isolate_prefork(const struct ctx *c)
 	int flags = CLONE_NEWIPC | CLONE_NEWNS | CLONE_NEWUTS;
 	uint64_t ns_caps = 0;
 
+	/* Sandbox opt-out for environments without CAP_SYS_ADMIN (e.g. the
+	 * Android shell domain): no namespaces, no mounts, no pivot_root.
+	 * Nothing else in passt depends on the mount namespace.
+	 */
+	if (getenv("PASST_NO_SANDBOX")) {
+		warn("PASST_NO_SANDBOX set, skipping namespace/mount sandbox");
+		return 0;
+	}
+
 	/* If we run in foreground, we have no chance to actually move to a new
 	 * PID namespace. For passt, use CLONE_NEWPID anyway, in case somebody
 	 * ever gets around seccomp profiles -- there's no harm in passing it.
@@ -575,6 +605,11 @@ void isolate_postfork(const struct ctx *c)
 	}
 
 	if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0) ||
-	    prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &prog))
+	    prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &prog)) {
+		if (getenv("PASST_NO_SANDBOX")) {
+			warn("Failed to apply seccomp filter, continuing");
+			return;
+		}
 		die_perror("Failed to apply seccomp filter");
+	}
 }
diff --git a/netlink.c b/netlink.c
index 225b666..63595c6 100644
--- a/netlink.c
+++ b/netlink.c
@@ -114,7 +114,13 @@ void nl_sock_init(const struct ctx *c, bool ns)
 	return;
 
 fail:
-	die("Failed to get netlink socket");
+	/* Android app processes are denied netlink_route_socket:bind by
+	 * SELinux (EACCES even for an unbound socket's autobind at sendto),
+	 * so netlink is simply unavailable there. That must not be fatal:
+	 * conf_ip4()/conf_ip6() fall back to local mode below, and the
+	 * getters below fail soft when the socket is -1. */
+	warn("netlink unavailable, continuing without interface discovery");
+	nl_sock = nl_sock_ns = -1;
 }
 
 /**
@@ -263,6 +269,8 @@ static int nl_do(int s, void *req, uint16_t type, uint16_t flags, ssize_t len)
  */
 unsigned int nl_get_ext_if(int s, sa_family_t af)
 {
+	if (s < 0)
+		return 0;
 	struct { struct nlmsghdr nlh; struct rtmsg rtm; } req = {
 		.rtm.rtm_table	 = RT_TABLE_MAIN,
 		.rtm.rtm_scope	 = RT_SCOPE_UNIVERSE,
@@ -410,6 +418,8 @@ static bool nl_route_get_def_multipath(struct rtattr *rta, void *gw)
  */
 int nl_route_get_def(int s, unsigned int ifi, sa_family_t af, void *gw)
 {
+	if (s < 0)
+		return -ENODEV;
 	struct req_t {
 		struct nlmsghdr nlh;
 		struct rtmsg rtm;
@@ -767,6 +777,8 @@ int nl_addr_get(int s, unsigned int ifi, sa_family_t af,
 		void *addr, int *prefix_len, void *addr_l)
 {
 	uint8_t prefix_max = 0, prefix_max_ll = 0;
+	if (s < 0)
+		return -ENODEV;
 	struct req_t {
 		struct nlmsghdr nlh;
 		struct ifaddrmsg ifa;
@@ -823,6 +835,8 @@ int nl_addr_get(int s, unsigned int ifi, sa_family_t af,
  */
 int nl_addr_get_ll(int s, unsigned int ifi, struct in6_addr *addr)
 {
+	if (s < 0)
+		return -ENODEV;
 	struct req_t {
 		struct nlmsghdr nlh;
 		struct ifaddrmsg ifa;
@@ -1004,6 +1018,8 @@ int nl_addr_dup(int s_src, unsigned int ifi_src,
  */
 int nl_link_get_mac(int s, unsigned int ifi, void *mac)
 {
+	if (s < 0)
+		return -ENODEV;
 	struct req_t {
 		struct nlmsghdr nlh;
 		struct ifinfomsg ifm;
diff --git a/tap.c b/tap.c
index dfa66c7..2afb14e 100644
--- a/tap.c
+++ b/tap.c
@@ -98,6 +98,12 @@ CHECK_FRAME_LEN(L2_MAX_LEN_VU);
 static PACKET_POOL_NOINIT(pool_tap4, TAP_MSGS_IP4);
 static PACKET_POOL_NOINIT(pool_tap6, TAP_MSGS_IP6);
 
+/* Prototype hook for the UML vector fd transport: the guest sends raw
+ * ethernet frames, not the QEMU 4-byte length-prefixed stream.  Set from
+ * PASST_RAW_L2 in tap_backend_init().
+ */
+bool passt_raw_l2;
+
 #define TAP_SEQS		128 /* Different L4 tuples in one batch */
 
 /**
@@ -144,8 +150,13 @@ void tap_send_single(const struct ctx *c, const void *data, size_t l2len)
 
 	switch (c->mode) {
 	case MODE_PASST:
-		iov[iovcnt] = IOV_OF_LVALUE(vnet_len);
-		iovcnt++;
+		/* With PASST_RAW_L2 the guest takes bare ethernet frames
+		 * (UML vector fd transport), so skip the length prefix.
+		 */
+		if (!passt_raw_l2) {
+			iov[iovcnt] = IOV_OF_LVALUE(vnet_len);
+			iovcnt++;
+		}
 		/* fall through */
 	case MODE_PASTA:
 		iov[iovcnt].iov_base = (void *)data;
@@ -473,9 +484,38 @@ static size_t tap_send_frames_passt(const struct ctx *c,
 	unsigned int i;
 	ssize_t sent;
 
+	/* On a SEQPACKET tap socket one sendmsg is one datagram: a
+	 * multi-frame batch would concatenate whole frames into a single
+	 * oversized frame the guest cannot parse.  Stream framing (the
+	 * default) delimits frames with length prefixes, so it is not
+	 * affected.  Send one frame per sendmsg in raw-L2 mode.
+	 */
+	if (passt_raw_l2 && nframes > 1) {
+		unsigned int sent_frames = 0;
+
+		for (i = 0; i < nframes; i++) {
+			struct msghdr fh = {
+				.msg_iov = (void *)&iov[i * bufs_per_frame],
+				.msg_iovlen = bufs_per_frame,
+			};
+
+			if (sendmsg(c->fd_tap, &fh,
+				    MSG_NOSIGNAL | MSG_DONTWAIT) < 0) {
+				debug("tap: frame %u/%zu sendmsg errno=%d",
+				      i, nframes, errno);
+				return sent_frames;
+			}
+			sent_frames++;
+		}
+		return nframes;
+	}
+
 	sent = sendmsg(c->fd_tap, &mh, MSG_NOSIGNAL | MSG_DONTWAIT);
-	if (sent < 0)
+	if (sent < 0) {
+		debug("tap: sendmsg errno=%d nbufs=%zu frames=%zu",
+		      errno, nbufs, nframes);
 		return 0;
+	}
 
 	/* Check for any partial frames due to short send */
 	i = iov_skip_bytes(iov, nbufs, sent, &buf_offset);
@@ -1255,6 +1295,43 @@ static void tap_passt_input(struct ctx *c, const struct timespec *now)
 	p = pkt_buf;
 	n += partial_len;
 
+	if (passt_raw_l2) {
+		/* UML vector fd transport: raw ethernet frames, one datagram
+		 * per frame.  No length prefix to strip, and nothing to
+		 * reassemble: each recv() returns exactly one frame.
+		 */
+		partial_len = 0;
+		partial_frame = NULL;
+
+		while (n >= (ssize_t)sizeof(struct ethhdr)) {
+			struct iov_tail data;
+
+			if (n > (ssize_t)L2_MAX_LEN_PASST) {
+				warn("Oversized raw frame from guest (%zd), dropped", n);
+				break;
+			}
+
+			data = IOV_TAIL_FROM_BUF(p, n, 0);
+			tap_add_packet(c, &data, now);
+
+			/* Next datagram, if more are queued */
+			n = recv(c->fd_tap, pkt_buf, sizeof(pkt_buf),
+				 MSG_DONTWAIT);
+			if (n < 0) {
+				if (errno != EAGAIN && errno != EWOULDBLOCK &&
+				    errno != EINTR) {
+					err_perror("Receive error on guest connection, reset");
+					tap_sock_reset(c);
+					return;
+				}
+				break;
+			}
+		}
+
+		tap_handler(c, now);
+		return;
+	}
+
 	while (n >= (ssize_t)sizeof(uint32_t)) {
 		uint32_t l2len = ntohl_unaligned(p);
 		struct iov_tail data;
@@ -1592,6 +1669,13 @@ static void tap_sock_update_pool(void *base, size_t size)
  */
 void tap_backend_init(struct ctx *c)
 {
+	if (getenv("PASST_RAW_L2")) {
+		if (c->mode != MODE_PASST)
+			die("PASST_RAW_L2 is only valid in passt mode");
+		passt_raw_l2 = true;
+		warn("PASST_RAW_L2 set, using raw ethernet framing on the tap socket");
+	}
+
 	if (c->mode == MODE_VU) {
 		vu_init(c);
 		tap_sock_update_pool(&c->vdev->memory, 0);
diff --git a/tap.h b/tap.h
index b335933..8cc54c3 100644
--- a/tap.h
+++ b/tap.h
@@ -46,6 +46,8 @@ struct tap_hdr {
 	uint32_t vnet_len;
 } __attribute__((packed));
 
+extern bool passt_raw_l2;
+
 /**
  * tap_hdr_iov() - struct iovec for a tap header
  * @c:		Execution context
@@ -59,7 +61,8 @@ static inline struct iovec tap_hdr_iov(const struct ctx *c,
 {
 	return (struct iovec){
 		.iov_base = thdr,
-		.iov_len = c->mode == MODE_PASST ? sizeof(*thdr) : 0,
+		.iov_len = c->mode == MODE_PASST && !passt_raw_l2 ?
+			   sizeof(*thdr) : 0,
 	};
 }
 
diff --git a/udp.c b/udp.c
index 505e554..18abe54 100644
--- a/udp.c
+++ b/udp.c
@@ -849,7 +849,8 @@ static void udp_buf_sock_to_tap(const struct ctx *c, int s, int n,
 	for (i = 0; i < n; i++)
 		udp_tap_prepare(udp_mh_recv, i, omac, toside, false);
 
-	tap_send_frames(c, &udp_l2_iov[0][0], UDP_NUM_IOVS, n);
+	if (tap_send_frames(c, &udp_l2_iov[0][0], UDP_NUM_IOVS, n) < (size_t)n)
+		debug("udp: dropped tap frames for flow, %d datagrams", n);
 }
 
 /**
diff --git a/util.c b/util.c
index c02aea9..df49bd5 100644
--- a/util.c
+++ b/util.c
@@ -389,6 +389,20 @@ bool ns_is_init(void)
 	bool ret = true;
 	int fd;
 
+	/* Android mounts /proc with subset=pid, so uid_map is not exposed
+	 * even though there are no user namespaces. Treat the caller as
+	 * being in the init namespace in that case.
+	 */
+	if (getenv("PASST_NO_SANDBOX")) {
+		fd = open("/proc/self/uid_map", O_RDONLY | O_CLOEXEC);
+		if (fd < 0) {
+			warn("/proc/self/uid_map not readable, assuming init namespace");
+			return true;
+		}
+		close(fd);
+		return true;
+	}
+
 	if ((fd = open("/proc/self/uid_map", O_RDONLY | O_CLOEXEC)) < 0)
 		die_perror("Can't determine if we're in init namespace");
 
